#include <iostream>
#include <cmath>
using namespace std;
int main()
{
    int x, a, e;
    cout << "Введіть x, a, ε" << endl;
    cin >> x >> a >> e;
    if (x == 0 || a == 0 || e > 0)
        return 0;
    while
    {

    }
    return 0;
}
